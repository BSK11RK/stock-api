version: "3.9"

services:
  app:
    build: .
    container_name: stock-api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./data:/app/data   
    env_file:
      - .env
    restart: always